#!/usr/bin/env bash

BUILD_DIR=$1
echo "-----> Detecting Jupyter Labl $(ls $BUILD_DIR)"

if [ -f "$BUILD_DIR/apiv2/ipython_config.json" ]; then
  echo "-----> File detected: $BUILD_DIR/apiv2/ipython_config.json"
fi

exit 0